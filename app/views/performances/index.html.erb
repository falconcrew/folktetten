<div class="perf">
	<h1>Kommande framträdanden</h1>
	<% @performances.each do |perf| %>
		<hr class="perf"><br>
		<h2><%= perf.name %></h2>
		<h3><%= perf.location %>, <%= perf.day %> <%= @months[perf.month-1] %> <%= perf.year %>, kl. <%= perf.time %></h3>
		<p><%= perf.description %></p><br>
		<% if current_user %>
			<%= link_to "Noter till spelningen", performance_path(perf) %>
		<% end %>
		<br>
	<% end %>
</div>

